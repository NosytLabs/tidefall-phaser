# Project status

Reviewed 2026-09-12. Tidefall is a real public Phaser browser game with
source, assets, specs, tests, build configuration, and an online deployment.

Quick check: `npm ci && npm test`; `npm run dev` starts the local game.

Readiness: playable public game, still an indie prototype rather than a live
service.
